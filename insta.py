import instaloader
import os
# Get instance
L = instaloader.Instaloader()

# Optionally, login or load session
USER = "cpane_lh"
PASSWORD = "stark@123"
L.login(USER, PASSWORD)# (login)
# (ask password on terminal)
# (load session created w/
